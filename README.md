#### clona el repocitorio

git clone y mi repot

luego ve a  cd backend y installa las dependencias (npm install)

y ejecuta (node server.js)

luego ve a cd landigpage-angular16
y haces lo mismo instalar las dependecnias con (npm install)

y luego correlo con (ng serve) y abre http://localhost:4200 ahi ya estara todo a la mano

